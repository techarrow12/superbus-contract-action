{
  "schema_version": 1,
  "mode": "write_allowed",
  "allowed_scope": ["src/profile/**", "tests/profile.test.ts"],
  "blocked_scope": ["src/payments/**", "src/auth/**"],
  "max_files": 4,
  "stop_conditions": [
    "Stop if payment processing files need changes",
    "Stop if more than four files need changes"
  ]
}
